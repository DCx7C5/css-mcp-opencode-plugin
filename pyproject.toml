[project]
name = "css-mcp-opencode-plugin"
version = "0.2.0"
description = "OpenCode ↔ Python bridge plugin: JS transport over a Unix socket + NDJSON test client"
requires-python = ">=3.14"
dependencies = [
    "msgspec>=0.21",
]

[dependency-groups]
test = [
    "pytest>=8",
    "ruff>=0.8",
]
